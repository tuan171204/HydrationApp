<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hydration.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="34" />

    <!-- Quyền truy cập Internet để gọi OpenWeather API -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Quyền gửi notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Quyền dùng AlarmManager để lên lịch nhắc nhở -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Quyền nhận broadcast khi máy khởi động (để restore alarm) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <permission
        android:name="com.hydration.app.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.hydration.app.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:allowBackup="true"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:debuggable="true"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:testOnly="true"
        android:theme="@style/Theme.HydrationApp" >

        <!-- MainActivity: màn hình chính -->
        <activity
            android:name="com.hydration.app.activities.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HistoryActivity: lịch sử uống nước -->
        <activity
            android:name="com.hydration.app.activities.HistoryActivity"
            android:exported="false"
            android:parentActivityName="com.hydration.app.activities.MainActivity" />

        <!-- ProfileActivity: hồ sơ người dùng -->
        <activity
            android:name="com.hydration.app.activities.ProfileActivity"
            android:exported="false"
            android:parentActivityName="com.hydration.app.activities.MainActivity" />

        <!-- StatsActivity: biểu đồ thống kê -->
        <activity
            android:name="com.hydration.app.activities.StatsActivity"
            android:exported="false"
            android:parentActivityName="com.hydration.app.activities.MainActivity" />

        <!--
            ReminderReceiver: BroadcastReceiver nhận alarm từ AlarmManager
            Đây là cách Android gửi notification - không hard-code giờ cố định
        -->
        <receiver
            android:name="com.hydration.app.receivers.ReminderReceiver"
            android:exported="false" />

        <!--
            BootReceiver: Khôi phục alarm sau khi máy khởi động lại
            Vì AlarmManager bị xóa khi tắt máy
        -->
        <receiver
            android:name="com.hydration.app.receivers.BootReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.hydration.app.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>